# Optional environment variables

# Dev server port (default: 3002, set in .env.defaults)
# PORT=3002

# Public editor origin used by Mint OAuth and same-origin checks.
# Set this when Pascal is hosted outside editor.pascal.app.
# MINT_PASCAL_HOST_ORIGIN=https://pascal.example.com

# Floor-plan → 3D import (apps/editor). OpenRouter is the local default.
# Put the live key in gitignored .env.local — not here.
# OPENROUTER_API_KEY=
# FLOORPLAN_VISION_MODEL=openai/gpt-4.1
#
# Fallbacks if OpenRouter is unset:
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
